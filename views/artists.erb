
<div class="button">
	<a href="/home">Take me home</a>
</div>

<div class="container">
	<div>
		<h2>Artists</h2>
		<h1>artists empty? <%= @@artists.empty? %></h1>
		<h1>artists nil? <%= @@artists.nil? %></h1>
		<h1>number of artists <%= @@artists.count %></h1>
		<ol>

		<%@artists.each do |artist_object|%>
			<li><a href="/artists/<%=artist_object.name.gsub(" ", "_")%>"><%=artist_object.name%></a> -- <%=artist_object.songs.size%> song(s)</li>
		<% end %>
		</ol>
	</div>

	<class
</div>